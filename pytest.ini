[pytest]
testpaths = tests
pythonpath = tests
addopts = -v --tb=short -m "not hardware"
markers =
    hardware: requires real hardware (camera, audio device). Run with: pytest -m hardware
    v4l2: requires a /dev/video* device to be present
